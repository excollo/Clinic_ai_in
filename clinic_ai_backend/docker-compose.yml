version: "3.9"
services:
  api:
    build:
      context: .
      dockerfile: deployments/docker/Dockerfile.api
    ports:
      - "8000:8000"
    env_file:
      - .env
  mongo:
    image: mongo:7
    ports:
      - "27017:27017"
