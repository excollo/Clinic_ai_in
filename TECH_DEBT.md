## Test suite (pre-existing failures)
- Full repo test suite has pre-existing failures unrelated to frontend 
  contract work. Needs a dedicated cleanup pass before CI is enabled.
  Do not block feature work on this.
